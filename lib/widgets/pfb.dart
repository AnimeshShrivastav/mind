import 'package:flutter/material.dart';

getPfb()
{
return 
	[
          TextButton.icon(icon: Icon(Icons.map), label: Text("map"), onPressed: () {}),
          TextButton.icon(icon: Icon(Icons.view_week), label: Text("week"), onPressed: () {}),
        ]
  ;
}